{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "com.memroam/memroam",
  "title": "Memroam",
  "description": "Portable AI memory that roams with you across models and harnesses — plain markdown you own, per-project spaces plus a shared layer.",
  "version": "0.3.1",
  "websiteUrl": "https://memroam.com",
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://memoryvault.click/mcp"
    }
  ],
  "packages": [
    {
      "registryType": "npm",
      "registryBaseUrl": "https://registry.npmjs.org",
      "identifier": "memroam",
      "version": "0.3.1",
      "runtimeHint": "npx",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MEMORY_DIR",
          "description": "Directory the vault stores memory in (defaults to ~/.memroam)",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "VAULT_PORT",
          "description": "Port the local server listens on (defaults to 8787)",
          "isRequired": false,
          "format": "number"
        }
      ]
    }
  ]
}
